// Test simple para verificar imports
try {
  console.log('Testing basic imports...');
  
  // This would work in a real Node.js context with proper module resolution
  console.log('✅ Test completed - check browser for real errors');
} catch (error) {
  console.error('❌ Import error:', error);
}
