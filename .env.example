# =====================================================
# Happy Balance - Variables de Entorno
# =====================================================
# Copia este archivo a .env y configura tus valores
# =====================================================

# === BASE DE DATOS ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=happy_balance

# === BACKEND ===
# Puerto del backend (por defecto 3004)
BACKEND_PORT=3004

# Seguridad JWT (IMPORTANTE: Cambiar en producción)
JWT_ACCESS_SECRET=tu-secret-key-super-segura-aqui
JWT_REFRESH_SECRET=tu-refresh-key-super-segura-aqui

# Usuario administrador inicial
ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambiar-esta-password-123

# === FRONTEND ===
# Puerto del frontend (por defecto 3000)
FRONTEND_PORT=3000

# URL de la API (ajustar según tu configuración)
# Para Docker local: http://localhost:3004/api
# Para producción: https://tu-dominio.com/api
VITE_API_URL=http://localhost:3004/api

# === CONFIGURACIÓN GENERAL ===
# Entorno (development | production)
NODE_ENV=production

# CORS - Orígenes permitidos (separados por comas)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173